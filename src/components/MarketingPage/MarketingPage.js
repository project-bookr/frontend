import React from 'react';




const MarketingPage=()=> {
    return (
        <div>
            <button onClick={() => this.props.history.push( "/dashboard" )}>
            <strong>
            Bookr
            </strong>
            </button>
            
        </div>
	);
}

export default MarketingPage;
